---
---

<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width,initial-scale=1.0" />
<title>Bobo Analytics</title>

<style>
body{
margin:0;
font-family:Arial;
background:linear-gradient(135deg,#020617,#2e026d);
color:white;
}

/* NAVBAR */
nav{
display:flex;
justify-content:space-between;
padding:15px 30px;
background:#020617;
position:sticky;
top:0;
z-index:1000;
}

.logo{
display:flex;
align-items:center;
gap:10px;
font-weight:bold;
}

.hero{
text-align:center;
padding:80px 20px;
}

.hero img{
width:420px;
border-radius:15px;
box-shadow:0 0 40px purple;
}

/* AI BUTTON */
#aiBtn{
position:fixed;
right:20px;
bottom:20px;
width:70px;
height:70px;
border-radius:50%;
background:linear-gradient(45deg,#22d3ee,#a855f7);
display:flex;
align-items:center;
justify-content:center;
cursor:pointer;
font-size:28px;
}

/* CHAT BOX */
#chatBox{
position:fixed;
right:20px;
bottom:100px;
width:320px;
height:420px;
background:#020617;
border-radius:12px;
display:none;
flex-direction:column;
overflow:hidden;
}

#messages{
flex:1;
padding:10px;
overflow:auto;
}

input{
border:none;
padding:12px;
width:100%;
}
</style>
</head>

<body>

<nav>
<div class="logo">🚀 Bobo Analytics</div>
<div>Services | Demo</div>
</nav>

<section class="hero">
<h1>AI Analytics Platform</h1>
<h2>Built for Modern Businesses</h2>
<p>Turn business data into intelligent decisions.</p>

<img src="/dashboard-image.png" />
</section>

<!-- AI CHAT -->
<div id="chatBox">
<div id="messages">
<b>AI Assistant:</b><br/>
Hi 👋 I'm Bobo Analytics AI.
</div>

<input
id="chatInput"
placeholder="Ask something..."
onkeypress="sendMessage(event)"
/>
</div>

<div id="aiBtn">🤖</div>

<script>
const btn=document.getElementById("aiBtn");
const chat=document.getElementById("chatBox");
const messages=document.getElementById("messages");

btn.onclick=()=> {
chat.style.display =
chat.style.display==="flex"?"none":"flex";
};

async function sendMessage(e){
if(e.key!=="Enter") return;

const input=e.target;
const text=input.value;
input.value="";

messages.innerHTML+=`<p><b>You:</b> ${text}</p>`;

const res=await fetch("/api/chat",{
method:"POST",
headers:{ "Content-Type":"application/json"},
body:JSON.stringify({message:text})
});

const data=await res.json();

messages.innerHTML+=`<p><b>AI:</b> ${data.reply}</p>`;
messages.scrollTop=messages.scrollHeight;
}
</script>

</body>
</html>
