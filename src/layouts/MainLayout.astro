---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
const { title = "Bobo Analytics" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <link rel="icon" type="image/png" href="/logo.png" />
    <style>
        :root {
            --bg-color: #05081a;
            --text-color: #ffffff;
            --accent-color: #6366f1;
        }
        * {
            box-sizing: border-box;
        }
        body {
            margin: 0;
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background: var(--bg-color);
            color: var(--text-color);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            overflow-x: hidden;
        }
        main {
            flex: 1;
        }
    </style>
</head>
<body>
    <div style="background: #ef4444; color: white; text-align: center; padding: 10px; font-weight: 900; z-index: 9999; position: relative;">
        🚀 DEPLOYMENT FIX ACTIVE - HOME BAKERS OS IS LIVE
    </div>
    <Navbar />
    <main>
        <slot />
    </main>
    <Footer />
</body>
</html>